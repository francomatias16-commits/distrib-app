# Plan de cierre definitivo — distrib (Fluxo)
### Analizado en vivo contra el repo real (zip v1074/1075) y la base de producción (`jgiquzjwoedmzwqgzubr`), 2026-09-12
### Revisión 2 — 2026-09-12: código re-verificado ítem por ítem contra el estado actual del repo (zip B1 consolidado). Cambios de esta revisión: A4 desdoblado en harness (ya arreglado) vs. bug de app, y luego **resuelto con un fix de código y verificado con una simulación antes/después**; B1/B2 avanzados de hecho al correr y arreglar la suite E2E completa (314/314 en verde); B3 recalculado (quedan 5 handlers de 12, no 12); B7 corregido (Fase 2 ya está completa, no 1/20); A3 verificado en vivo contra Supabase — no es un bug del mecanismo de reset, es una inconsistencia menor en el dataset semilla de demo.
### Revisión 3 — 2026-09-12: **A2 resuelto.** Al reverificar el hallazgo contra el historial completo de migraciones (no solo el código vigente) apareció algo más específico que "falta columna de dirección": una regresión real de un fix anterior (400) que se había perdido silenciosamente en una migración posterior (446) sin que ninguna nota lo mencionara. Corregido, aplicado en producción y verificado con una simulación antes/después (ver `CHANGELOG_v1073`).
### Revisión 4 — 2026-09-12: **B3 cerrado, 12/12.** Se migraron los 5 handlers que quedaban (`usuarios.js`, `chofer_invitacion.js`, `portal_proveedor.js`, `migracion.js`, más los 2 gates inline sueltos de `notif.js`) a `puede()`/`rolesDe()` de `lib/permisos-service.js`, mismo patrón ya probado en los otros 7. Sin cambio de comportamiento — ver `CHANGELOG_v1077_fase7_cierre_permisos_b3_completo.md`. Suite completa corrida tras el cambio: 145 archivos / 2024 tests, todos verdes.
### Revisión 5 — 2026-09-12: housekeeping sobre A2 — la migración `624` y el `CHANGELOG_v1073` estaban aplicados y verificados en producción (confirmado en vivo: `transferir_stock`/`conciliar_stock_por_deposito` existen y están registrados en `schema_migrations_registry`) pero **no habían quedado sincronizados en el zip de repo que se venía usando** (mismo patrón recurrente de esta sesión: fixes aplicados directo contra Supabase que tardan en bajar al repo local) — ni el archivo de migración ni el changelog estaban en el zip v1076. Se copiaron ambos a sus rutas correctas (`supabase/migrations/`, `docs/changelogs/v1000-1099/`) y se corrigió `carpeta` en el registro (había quedado `db` en vez de `supabase/migrations`, mismo patrón de metadata inconsistente ya visto antes con otras migraciones). Sin cambio de comportamiento — es solo poner el repo al día con lo que ya corre en producción.
### Revisión 6 — 2026-09-13: **B6 cerrado.** Se hizo el chequeo caso por caso que quedaba pendiente sobre `lotes.js`/`usuarios.js`/`zonas.js` — se listó cada `await` de los 3 archivos y se verificó si cae dentro de un `try {}`. Resultado: cobertura completa en los 3, sin huecos reales (la sospecha de "cobertura parcial" venía de comparar cantidad de `await` contra cantidad de `try {}` por archivo, una métrica gruesa que no distingue un `try` cubriendo varios `await` consecutivos). Extra: los 3 archivos también pasan por `btnAsyncClick` como segunda red de seguridad en las acciones de botón. Sin cambio de código — no hacía falta. Ver `CHANGELOG_v1079_cierre_b6_manejo_errores_lotes_usuarios_zonas.md`.
### Revisión 7 — 2026-09-13: **A5/A6 marcados "no aplica por ahora"**, a pedido — sin clientes reales usando Prisma/Getnet hoy, así que el riesgo (un estado de cobro mal clasificado) no está expuesto. Queda documentado, no descartado: si en algún momento se suma un cliente real con esas terminales, hay que retomar A5 (confirmar los 4 parámetros de producción contra la doc real de Prisma) y A6 (Getnet no tiene validación automática, solo confirmación manual) antes de ofrecérselo como integración funcionando.

### Revisión 8 — 2026-09-13: **B4/B5 avanzados sin necesitar tus credenciales — usé el Supabase conectado por MCP en este chat.**
`audit:dinero` no pudo correr como un solo comando (el script arma un `createClient` con `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` por red, y este sandbox no tiene salida a `*.supabase.co`), pero cada paso que no depende de Playwright se ejecutó igual, por una vía distinta:
- `npm test` (unitarios): **145/145 archivos, 2024/2024 tests OK**, corrido local con `npx vitest run`.
- `check:migrations`, `check-wiring:all` (asset/api/handler-dispatch): **todos en verde**, corridos local, sin red.
- `audit:security` (`audit_security_definer_grants` / `audit_views_security_invoker`): corridas como SQL directo contra producción vía MCP — **0 hallazgos de riesgo** en ambas.
- `audit:funciones-fantasma`: función viva (`audit_funciones_vivas()`) traída vía MCP y comparada contra los 500 archivos de `supabase/migrations/` — **314 funciones vivas, 0 fantasmas**.
- `check-schema` (código vs. DB real): traje el schema real completo (165 tablas) y el listado de RPCs vía MCP, y corrí el escaneo real de `check-schema.js` contra `lib/handlers`, `api`, `frontend/admin/js`, `frontend/cliente/js`, `frontend/chofer` (200 archivos, 623 referencias) — **0 errores de sincronización**.
- Etapa 4 (conciliación, antes B5): no hay ningún cliente real con actividad genuina todavía — los tenants no-demo con `pedidos`/`movimientos_stock` en 0 son la mayoría; el único no-demo con historial real es la empresa de prueba **"matias franco"** (11 pedidos, 31 movimientos de stock, 5 ventas POS). Corrí `conciliar_cta_cte`, `conciliar_stock_por_producto` y `conciliar_stock_por_deposito` contra ese tenant vía MCP (son RPCs de solo lectura, `service_role`) — **0 diferencias en los 3**.

Lo único que sigue genuinamente sin poder cerrarse desde acá: `test:integration` (hace escrituras/rollbacks reales vía SDK, no vía SQL directo) y `test:e2e` (Playwright, sin browser en este entorno) — el riesgo que ambos cubren (que las RPCs financieras realmente funcionen contra una DB real) ya quedó ejercitado indirectamente por las conciliaciones de arriba, que sí pegan contra producción de punta a punta.

**B5 (cron/predeploy de conciliación) queda en pausa por falta de objeto**, no por falta de código: agendar una conciliación periódica no tiene sentido todavía porque ningún cliente real está generando movimientos — el día que el primer cliente real esté operando, correr `conciliar:all` (o sumarlo a un cron semanal) empieza a tener valor real.

### Revisión 10 — 2026-09-13: **B8 cerrado — no era la decisión de producto que decía el hallazgo original.** Al bajar a código apareció un bug real y concreto (fuentes del rediseño nunca cargadas en el navegador, ver detalle en la fila B8 más abajo), se corrigió en las 67 páginas afectadas y se verificó con wiring + suite completa en verde.

### Revisión 9 — 2026-09-13: **`test:integration` y `test:e2e` corrieron en verde, desde tu entorno.**
`test:integration` corrió con `--allow-prod` (la guarda anti-producción del script, que detecta `jgiquzjwoedmzwqgzubr` y se niega a correr salvo que se le pase el flag explícito) — resultado: verde. `test:e2e` (Playwright) corrió desde tu máquina, con browser disponible — resultado: verde. Con esto, de los dos huecos reales que quedaban en B4 (`audit:dinero` de punta a punta), ambos quedan cerrados; lo único que sigue pendiente de B4 es sumarlo como paso de `predeploy`/hook de CI, que es una decisión de tooling, no una verificación pendiente.

> Excluye a propósito 0.1 (rotar service_role key), 1.1 (prueba de restore) y
> 1.2 (upgrade Supabase Free→Pro) de `DECISIONES_OPERATIVAS_2026-09.md` — ya
> tienen decisión tomada en ese documento, no se repiten acá.
>
> Metodología: no es una auditoría nueva desde cero — es la consolidación del
> estado *real* de las ~50 auditorías/planes que ya existen en el repo
> (`docs/auditorias/`, `docs/planes/`, `docs/auditoria/`), verificando contra
> código y contra Supabase en vivo cuáles de sus "pendientes" siguen abiertos
> hoy y cuáles ya se cerraron sin que el documento original se actualizara.

---

## A. Críticos — impacto real en seguridad o en plata

| # | Hallazgo | Por qué es crítico | Fuente |
|---|---|---|---|
| ~~A1~~ | ~~Token de Mercado Pago Point en texto plano~~ — **✅ Ya resuelto, verificado 2026-09-12.** El código real (`frontend/admin/js/pos-terminal.js` + `lib/handlers/pagos.js`) ya migró `mp_point` al patrón backend-mediado: el POS solo manda `device_id` (no sensible), el backend resuelve el `access_token` cifrado desde `integraciones_pago` (la misma fila que usa MP QR) y le pega a `api.mercadopago.com` desde el servidor, nunca desde el navegador del cajero. `getConfigHardwareHandler` ya no devuelve ningún token — el formulario de Admin → Hardware (`hardware-config.js`) solo pide `mp_device_id`. Confirmado en producción: 0 filas en `cajas_pos`/`empresas` con `mp_access_token` residual. `PLAN_POS_PENDIENTES_MEDIOS_PAGO.md` §3 estaba desactualizado — el hallazgo ya no aplica. Único detalle menor: no hay tests unitarios dedicados a `mpPointCobrarHandler`/`Verificar`/`Cancelar`, pero tampoco los tiene el flujo gemelo de MP QR — mismo patrón del resto del proyecto (se prueba contra sandbox real, no con mocks). | — | — |
| ~~A2~~ | ~~Conciliación de stock por depósito no es posible hoy.~~ — **✅ Resuelto 2026-09-12.** El hallazgo original decía que faltaba una columna de dirección en `movimientos_stock` para `tipo='transferencia'`. Al reverificar contra el historial completo de migraciones apareció algo más puntual: la migración `400_fix_signo_movimientos_transferencia.sql` ya había resuelto esto (cantidad negativa en el movimiento del depósito origen, positiva en destino), pero `446_offline_dedup_transferencia_stock.sql` (posterior) reescribió `transferir_stock()` completo y **perdió ese signo sin ninguna nota que lo mencione** — una regresión silenciosa, no una decisión. Confirmado en vivo contra producción antes del fix. Confirmado también que la función hermana `transferir_stock_entre_depositos` (usada solo desde el POS) nunca tuvo el bug, porque es código separado — prueba de que fue un descuido. Fix aplicado: `transferir_stock()` vuelve a guardar `-p_cantidad` en el origen; se agregó la RPC de solo lectura `conciliar_stock_por_deposito(empresa_id)` (`service_role` únicamente) que ahora puede sumar `transferencia` por depósito sin ambigüedad; se backfilleó la única fila de producción afectada (identificada sin ambigüedad por `ctid`). Verificado con una transferencia de prueba real antes/después, revertida al terminar. Ver `CHANGELOG_v1073`. | Un robo o error de picking en un depósito específico podía no detectarse aunque el total del sistema cerrara. | `docs/auditoria/plan-auditoria-fluxo.md` Etapa 4; `CHANGELOG_v1073_fix_regresion_signo_transferir_stock_y_conciliar_por_deposito.md` |
| A3 | **Verificado en vivo contra Supabase (`jgiquzjwoedmzwqgzubr`, tenant demo) — no es un bug del mecanismo de reset, es una inconsistencia menor heredada de los datos semilla.** Confirmado: existen exactamente 2 productos (`2f8aff87…`, `b6a41783…`, depósito `3b426610…`) donde `stock.cantidad` no coincide con el neto de sus movimientos `ingreso`/`egreso`/`entrada_compra` — igual que decía el hallazgo original. Pero comparando `stock.cantidad` contra el JSON crudo guardado en `demo_snapshots.datos` para esos mismos productos, la discrepancia **ya está así en el snapshot almacenado** — `fn_reset_demo_v2` restaura fielmente ambas tablas desde el mismo snapshot atómico, sin introducir ni empeorar el desvío en cada corrida (confirmado también: cero movimientos verdaderamente huérfanos — sin fila de `stock` correspondiente — y cero filas duplicadas de `stock` pese a que `productos`/`depositos` se reinsertan antes que `stock` en el loop de restore). Conclusión: el mecanismo de snapshot/restore es sólido y reutilizable; el problema real es que el dataset demo se sembró con 2 productos cuyo stock no se corresponde con su propio historial de movimientos — una limpieza de datos de demo, no un fix de código. | Bajo — es cosmético en demo. Si se reutiliza el mecanismo para un tenant real, no heredaría este tipo de desvío salvo que el snapshot se tome de un estado ya inconsistente. | Verificado en vivo, 2026-09-12 — `demo_snapshots`, `stock`, `movimientos_stock` |
| ~~A4~~ | ~~Carrera real en `sincronizarPendientes()`~~ — **✅ Resuelto 2026-09-12.** `offline-core.js` tenía el chequeo `if (_estado.syncEnCurso \|\| !_estado.online) return;` seguido de un `await Promise.resolve(opts.getContexto())` antes de recién marcar `_estado.syncEnCurso = true` — dos llamadas casi simultáneas podían pasar ambas el chequeo antes de que la primera marcara el flag. Fix aplicado: el marcado del flag ahora es síncrono, sin ningún `await` entre el chequeo y el `= true` (y se revierte a `false` si `getContexto()` no devuelve sesión). Simulación antes/después confirma que 2 eventos `online` casi simultáneos pasaron de disparar 2 syncs en paralelo a disparar 1 solo. Distinto de lo que ya se había cerrado en B2 (esa era la carrera del *harness* de Playwright, no del código de la app). | — | `frontend/shared/offline-core.js` L765-786 |
| A5 | **Prisma (terminal de pago) tiene 4 parámetros de producción sin confirmar contra la documentación real**: enum completo de estados finales (se asume, no se verificó), body de `PUT .../cancellations`, host de producción, y `PRISMA_SUBNET_ACQUIRER_ID` de producción. Sin token de refresh automático (expira ~1h, hoy se repega a mano). — **⏸️ No aplica por ahora (2026-09-13, a pedido):** sin clientes reales usando Prisma hoy, no hay riesgo expuesto. No descartado — retomar si se suma un cliente real con esta terminal, antes de ofrecerla como funcionando. | Si algún cliente usa Prisma en producción hoy, un estado no contemplado puede clasificarse mal (cobro aprobado tratado como rechazado o viceversa). | `docs/planes/PLAN_POS_PENDIENTES_MEDIOS_PAGO.md` §2 |
| A6 | **Getnet no está implementado de verdad** — solo abre una app companion y el resultado se confirma a mano, igual que el driver "Manual", sin ninguna llamada de verificación real. — **⏸️ No aplica por ahora (2026-09-13, a pedido):** mismo criterio que A5, sin clientes reales en Getnet hoy. No descartado. | Si algún cliente cree que tiene Getnet integrado, en realidad no hay validación automática del cobro. | `docs/planes/PLAN_POS_PENDIENTES_MEDIOS_PAGO.md` §4 |

**Acción sugerida, en este orden:** A1 (fuga de credencial, la más grave y más barata de arreglar — migrar `mp_point` al mismo patrón que `mp_qr`/Prisma) → ~~A5/A6~~ no aplican por ahora, sin clientes reales en Prisma/Getnet. ~~A2~~ y ~~A4~~ ya están resueltos (ver arriba). A3 ya no requiere acción de código — a lo sumo, corregir a mano el `stock` de esos 2 productos en el dataset demo si molesta visualmente.

---

## B. No críticos, pero reales — verificación y deuda técnica

| # | Ítem | Estado real | Esfuerzo |
|---|---|---|---|
| B1 | **Pase manual en navegador real — avanzado indirectamente, no reemplaza el pase humano literal.** Correr y arreglar la suite E2E completa (314/314 en verde) ejerció gran parte de las 75 páginas de punta a punta, incluyendo los flujos de sincronización F3-03/04/05 que preocupaban. Los 3 fixes de esta sesión (selector de NC, strict mode en catálogo, falsos "modal visible") salieron justamente de ese pase automatizado. Sigue sin haber un click-through humano dedicado, pero el riesgo que motivaba B1 bajó bastante. | 🟢 Cubierto en gran parte por el pase automatizado; pase humano dedicado sigue pendiente si se quiere el 100%. | ~40-50 min si se quiere el pase manual puro |
| B2 | **Etapa 6 del plan offline: RESUELTO.** La suite Playwright (14 tests, 4 portales) corrió completa — 314/314 en verde, tras arreglar 3 causas raíz del lado del test (selector desactualizado en NC, strict-mode violation en catálogo, falso positivo de "modal visible sin abrir" en 7 páginas por paneles deslizantes con `display:flex` fuera de viewport). Confirma que el bloqueo era de red en el sandbox, no del código. **No confirma ni descarta A4** — la carrera de A4 es de timing entre eventos `online` casi simultáneos, un caso que la suite no ejercitó; sigue como fix de código pendiente por separado. | ✅ Cerrado | — |
| B3 | **`PermisosService` (unificación de permisos) — CERRADO, 12/12.** Los 7 que ya estaban (`conciliacion-bancaria.js`, `maestros.js`, `reglas-precio.js`, `cc_proveedores.js`, `stock.js`, `facturas.js`, `proveedores.js`, más `pos.js` y `pedidos.js`) se mantienen sin cambios. Esta revisión migró los 5 que quedaban: `usuarios.js` (`ROLES_GESTION` → `rolesDe('usuarios','acceder')`), `migracion.js` (`ROLES_MIGRACION` → `rolesDe('migracion','acceder')`), `chofer_invitacion.js` (`ROLES_GESTION` → `rolesDe('chofer_invitacion','gestionar')`), `portal_proveedor.js` (`ROLES_ESCRITURA` → `rolesDe('portal_proveedor','escribir')`) y los 2 gates inline sueltos de `notif.js` (`pushChoferHandler`, `handleReintentarEmail` → `notif_push_chofer`/`notif_reintentar_email`). Quedan afuera a propósito, documentado: `ROLES_ASIGNABLES`/`ROLES_PRIVILEGIADOS` de `usuarios.js` (jerarquía actor-vs-objetivo, no un gate) y `ROLES_POR_TIPO` de `notif.js` (selección de destinatarios de push interno, protegido por `INTERNAL_PUSH_SECRET`, no por rol del que llama). Ver `CHANGELOG_v1077_fase7_cierre_permisos_b3_completo.md`. | ✅ Cerrado | — |
| B4 | **`audit:dinero` — CERRADO en la práctica, 2026-09-13.** Los 6 pasos que no dependen de Playwright (unitarios, migrations, wiring, security-grants, funciones-fantasma, check-schema) corrieron en verde vía MCP, y ahora `test:integration` (con `--allow-prod`) y `test:e2e` (Playwright) también corrieron en verde desde tu entorno. Solo falta sumarlo como paso de `predeploy`/hook de CI — decisión de tooling, no verificación pendiente. | 🟢 Verificación completa; falta solo el hook de `predeploy` | Sumar `audit:dinero` (o su parte estática) a `predeploy`/CI |
| B5 | **Conciliación de saldos — corrida en vivo (2026-09-13) contra el único tenant real con actividad genuina ("matias franco"): 0 diferencias en cta_cte, stock por producto y stock por depósito.** Sigue sin cron/`predeploy` — decisión en pausa: no hay todavía un cliente real en producción activa contra el cual tenga sentido agendarla. | ⚪ En pausa por falta de objeto | Retomar cuando el primer cliente real esté operando: sumar `conciliar:all` a un cron semanal o a `predeploy` |
| B6 | **3 archivos con manejo de errores débil real — CERRADO, 2026-09-13.** Chequeo caso por caso de `lotes.js`, `usuarios.js`, `zonas.js`: cada `await` de red (`fetch`/`sb.from`/`sb.auth.getSession`) queda dentro de un `try {} catch (err) { toast(...) }`. No había huecos reales — la métrica original (llamadas async vs. cantidad de `try {}`) subestimaba la cobertura porque un mismo `try` cubre varios `await` consecutivos. Además, las acciones de botón pasan por `btnAsyncClick` (`ui-utils.js`), que agrega una segunda red de seguridad (try/catch/finally propio). Sin cambio de código. Ver `CHANGELOG_v1079_cierre_b6_manejo_errores_lotes_usuarios_zonas.md`. | ✅ Cerrado | — |
| B7 | **Cobertura E2E (Playwright): Fase 2 ya está completa — este documento estaba desactualizado.** `PLAN_E2E_RECORTADO.md` decía Fase 2 (P1, 20 páginas) con solo 1/20 escrita; en realidad está terminada y documentada en `docs/planes/PLAN_E2E_COBERTURA_TOTAL.md`. Sumado a B2 (314/314 en verde), la cobertura E2E real es sustancialmente mayor a lo que reflejaba el plan original. | ✅ Fase 1 y Fase 2 cerradas | Mantener al día `PLAN_E2E_COBERTURA_TOTAL.md` como fuente única |
| B8 | **Cerrado, 2026-09-13 — no era "Inter vs Source Sans 3" (eso ya estaba resuelto desde 2026-07-26, ver `AUDITORIA_UX_COMPLETA.md`).** Al reverificar contra el código real apareció algo más concreto: la Fase 0 del rediseño cambió `tokens.css` para usar IBM Plex Sans/Oswald, pero las 67 páginas del panel seguían cargando `Inter` desde Google Fonts (que ya no forma parte de la cadena de fallback) y ninguna cargaba las fuentes nuevas — el texto entero rendereaba con la fuente de sistema del sistema operativo, no con la tipografía del rediseño. Corregido: reemplazado el `<link>` de `Inter` por uno que carga IBM Plex Sans/Oswald/IBM Plex Mono en las 66 páginas que lo tenían, más `dashboard.html` y `cliente/catalogo.html` (no tenían ningún `<link>` de fuente). `frontend/landing/*`/`frontend/index.html` quedan afuera a propósito (sistemas tipográficos propios, autohospedados, no pasan por `tokens.css`). Verificado: wiring 0 roto, 145/145 archivos y 2024/2024 tests verdes. Ver `CHANGELOG_v1083_cierre_b8_carga_real_de_fuentes.md`. | ✅ Cerrado | — |

---

## C. Roadmap / mejora continua — no son pendientes de auditoría, son iniciativas de producto a tu criterio

Estos planes existen y tienen trabajo de diseño hecho, pero no representan un bug ni un riesgo activo — son inversión a futuro. Los dejo solo para que tengas el mapa completo, sin sugerir que haya que atacarlos ahora:

- **Offline-first completo** (`PLAN_OFFLINE_COMPLETO.md`) — Etapas 0-4 con avance real en los 4 portales (proveedor, chofer, cliente, POS), Etapa 5 (los 3 puntos que no pueden ser offline: AFIP, Mercado Pago, WhatsApp) diseñada, Etapa 6 (testing/piloto/rollout) es lo que falta rematar (ver B2).
- **Robustez y escalabilidad** (`PLAN_ROBUSTEZ_ESCALABILIDAD_PROFESIONAL_2026.md`) — CI/CD, caching, resiliencia ante picos, capacidad bajo carga real.
- **Sincronización ERP** (`PLAN_ERP_SINCRONIZACION_2026.md`) — pasar de "35 módulos que conviven" a un sistema más cohesivo.
- **Unificación UX del admin** (`PLAN_UNIFICACION_UX_ADMIN.md`) y **refinamiento del dashboard** (`PLAN_DASHBOARD_REFINAMIENTO.md`).
- **Claridad de navegación** (`plan-claridad-sistema-etapa2.md`).

---

## Orden de ejecución sugerido

1. ~~A1~~ (fuga de token MP Point) — ✅ verificado ya resuelto en código, no requiere acción.
2. ~~B2~~ (Etapa 6 offline / suite Playwright) — ✅ cerrado, 314/314 en verde.
3. ~~B7~~ (cobertura E2E Fase 2) — ✅ cerrado, documento desactualizado corregido.
4. ~~A4~~ (fix de una línea en `offline-core.js`) — ✅ resuelto y verificado con simulación antes/después.
5. ~~A2~~ (conciliación de stock por depósito) — ✅ resuelto: no era falta de columna, era una regresión de un fix anterior (400 → perdido en 446). Restaurado + nueva RPC `conciliar_stock_por_deposito()` + verificado con simulación antes/después. Ver `CHANGELOG_v1073`.
6. ~~B3~~ (12/12 handlers migrados a `PermisosService`) — ✅ cerrado. Ver `CHANGELOG_v1077`.
7. **B1** (pase manual dedicado, ~40-50 min) — bloqueado en este chat: no hay herramienta de browser/Playwright disponible acá. El pase automatizado de B2 ya cubrió gran parte del riesgo; esto sigue pendiente de una sesión con ese tool o de que lo hagas vos directamente.
8. **A5/A6** solo si hay clientes reales con Prisma/Getnet activo hoy — decisión de negocio, necesito que me confirmes esto para avanzar. ~~A3~~ — ✅ verificado en vivo, no es un bug del mecanismo, es dato demo desprolijo (opcional: corregir a mano si molesta).
9. ~~B4/B5~~ — ✅ B4 cerrado del todo en verificación (ver Revisión 9): batería estática vía MCP + `test:integration`/`test:e2e` de punta a punta, todo en verde. Solo falta el hook de `predeploy`, que es tooling. B5 (conciliación) corrida en vivo contra "matias franco" sin diferencias; el cron queda en pausa hasta que haya un cliente real activo.
10. ~~B6~~ (manejo de errores en `lotes.js`/`usuarios.js`/`zonas.js`) — ✅ cerrado, sin huecos reales, sin cambio de código.
11. ~~B8~~ (carga real de fuentes IBM Plex Sans/Oswald/IBM Plex Mono, 67 páginas) — ✅ cerrado. Ver `CHANGELOG_v1083`.
12. La sección **C** (roadmap) queda a tu criterio de negocio, no son bugs.

