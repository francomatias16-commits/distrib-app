# Auditoría Integral — Distrib SaaS (v295)
**Inicio:** 2026-07-11
**Proyecto Supabase:** `jgiquzjwoedmzwqgzubr` (Postgres 17.6, us-west-2, ACTIVE_HEALTHY)
**Alcance:** Backend (Vercel/Node), Supabase (DB + RLS + funciones), Frontend (admin/cliente/chofer/proveedor), integraciones (AFIP/ARCA, WhatsApp, email, pagos)

Este documento es el **tablero de seguimiento**. Cada etapa tiene su propio archivo en `AUDITORIA_2026/etapas/` con el detalle de hallazgos. Acá solo se registra el estado y los pendientes críticos para que nada se pierda entre sesiones.

---

## Estado general

| # | Etapa | Estado | Archivo | Hallazgos críticos abiertos |
|---|-------|--------|---------|------------------------------|
| 1 | Inventario y superficie de ataque | 🟢 Completa | `etapas/01_inventario.md` | 0 |
| 2 | Seguridad de base de datos (RLS, SECURITY DEFINER, grants) | 🟢 **Completa (2026-08-25)** — todos los hallazgos accionables están corregidos, incluyendo SEC-004 (extensiones movidas a schema `extensions`, verificado sin roturas); solo queda SEC-003, bloqueado por el plan Free de Supabase (no accionable, no es un pendiente técnico) | `etapas/02_seguridad_db.md` | 0 |
| 3 | Backend / API (handlers) | 🟢 Barrido de auth completo (34/34) | `etapas/03_backend_api.md` | 0 |
| 4 | Integraciones externas (AFIP, WhatsApp, email, pagos) | 🟢 Completa | `etapas/04_integraciones.md` | 0 |
| 5 | Frontend | 🟢 Completa (foco XSS) — 8 hallazgos corregidos, 0 abiertos | `etapas/05_frontend.md` | 0 |
| 6 | Consistencia end-to-end y robustez | 🟢 Cerrada (CONS-01/02/03/04 corregidos y verificados; hallazgo suelto #21 — wizard de migración — cerrado 2026-08-24) | `etapas/06_consistencia_e2e.md` | 0 |
| 7 | Performance y escalabilidad (queries, índices, N+1) | 🟢 **Cerrada 2026-07-19** — PERF-04 resuelto: `DROP SCHEMA backup_pre_wipe_20260702` (v402). PERF-06 resuelto: 5 índices para FKs sin cobertura (v404) + 4 políticas RLS con `(select auth.uid())` en puntos/recompensas (v403). PERF-05 resuelto en las 43/43 tablas afectadas (v405/v406/v407): 271 hallazgos `multiple_permissive_policies` → 0 en las tablas de este plan, verificado con Advisors. PERF-07 resuelto (v407): decisión de negocio confirmada, ver detalle abajo. Además, fuera de este plan: 4 vistas `SECURITY DEFINER` con acceso indebido a anon/authenticated corregidas (v401). | `etapas/07_performance_escalabilidad.md` | 0 |
| 8 | Observabilidad (logging, monitoreo, alertas de error) | 🟡 Cerrada con 1 pendiente de usuario | `etapas/08_observabilidad.md` | **OBS-03: fila de `internal_secrets` se perdió/nunca se cargó — regenerada 2026-08-25, falta setear `INTERNAL_PUSH_SECRET` en Vercel con el mismo valor y redesplegar** |
| 9 | Backups y disaster recovery | 🟢 Cerrada (2026-08-25) — secrets cargados por el usuario, backup semanal automatizado activo | `etapas/09_backups_disaster_recovery.md`, `etapas/09b_backup_automatizado_setup.md` | 0 |
| 10 | Dependencias / supply chain (npm) | 🟢 Cerrada (0 críticas/altas, 1 moderada no explotable en este proyecto, 1 hallazgo de reproducibilidad de build) | `etapas/10_dependencias_supply_chain.md` | 0 |
| 11 | Rate limiting y protección DoS | 🟢 **Cerrada de verdad (confirmado 2026-08-25)** — reintento posterior (v982, `20260817_p2_lote3_webhook_ratelimit_push_xss.sql`) resolvió la causa del incidente de v297 agregando fallback a Map local si Supabase no responde; verificado en vivo: `public.rate_limits` con 124 filas, última actualización 2026-08-25 01:47 UTC — el contador distribuido está activo en producción, no es el Map en memoria | `etapas/11_rate_limiting_dos.md` | 0 |
| 12 | Riesgo de cheques | 🟢 Cerrada — módulo en sí sin hallazgos; investigándolo se encontraron 2 hallazgos colaterales (login bloqueaba vendedor/depositero/contador; 4 acciones de `score.js` sin chequeo de rol) | `etapas/12_riesgo_cheques.md` | 0 |

Leyenda: 🟢 completa · 🟡 en progreso · 🔴 con hallazgos críticos sin resolver · ⚪ no iniciada

_Etapas 7-11 agregadas 2026-07-11 (sesión 10) a pedido del usuario, para extender el alcance de la auditoría más allá del plan original (1-6). Alcance de cada una definido en su propio archivo al llegar a ella._

---

## Hallazgos críticos / altos (vista rápida, se detallan en cada etapa)

| ID | Severidad | Hallazgo | Etapa | Estado |
|----|-----------|----------|-------|--------|
| RL-01 | 🟢 Resuelta | **Todo el rate limiting HTTP (`lib/rate-limit.js`) usaba un `Map` en memoria por instancia de Vercel, no compartido** — bajo un ataque real (que escala instancias en paralelo), el límite efectivo se multiplicaba. El primer intento (v297, `fn_rate_limit_check`) tumbó `/admin/dashboard` (504 en 9 endpoints, revertido en v303 — ver historial abajo). | 11 | ✅ **Corregido de verdad (v982, migración `20260817_p2_lote3_webhook_ratelimit_push_xss.sql`)** — nueva función `rl_check_and_increment` sobre tabla `public.rate_limits` (INSERT...ON CONFLICT atómico, sin ventana de carrera) que además **degrada a Map local con log si Supabase no responde**, en vez de colgar el request como hacía `fn_rate_limit_check` — esa es la diferencia que evita repetir el incidente de v297/v300. Verificado en vivo el 2026-08-25: 124 filas en `rate_limits`, última actualización hace minutos. `rateLimitAuth`/`rateLimitApi`/`rateLimitPorClave` todos pasan por este mecanismo ahora. |
| RL-02 | 🟡 Informativo (riesgo de negocio, no bug) | El proyecto corre en **Vercel Hobby** (confirmado por el propio código: dispatcher único por el límite de 12 funciones del plan gratuito) — mismo patrón de riesgo que BACKUP-01: Hobby prohíbe uso comercial en su ToS y tiene reglas de WAF/rate-limiting de edge limitadas o pagas. | 11 | 🟢 **Decidido (2026-07-11): sin presupuesto para upgrades por el momento — seguís en Hobby.** Riesgo de ToS aceptado conscientemente. Mitigación gratuita disponible: resolver RL-01 con un contador centralizado en Supabase (sin costo) en vez de Redis/Upstash. |
| **BACKUP-01** | 🟡 **Mitigado — activo, falta probar restauración** | **El proyecto Supabase está en plan Free: cero backups automáticos nativos, sin PITR, sin SLA.** Confirmado vía API (`get_organization`) y documentación oficial de Supabase. Hay datos financieros reales (cta_cte, facturas, cobros, cheques) que dependían de esto. | 9 | 🟢 **Activo en producción (2026-08-25, corrección de una nota desactualizada en esta misma fila):** `.github/workflows/backup-supabase.yml` corre `pg_dump` semanal (domingos), cifrado con GPG (AES256), subido como artifact de GitHub Actions (retención 90 días). Los 3 secrets ya están cargados (ver `etapas/09b_backup_automatizado_setup.md` — reemplazaron el viejo `SUPABASE_DB_URL` único por `SUPABASE_DB_HOST/USER/PASSWORD` + `BACKUP_GPG_PASSPHRASE`). **Único punto real que sigue pendiente:** probar una restauración completa contra un proyecto Supabase de prueba — nunca se hizo, y un backup no probado no es un backup confiable. Sigue sin PITR (pérdida máx. de 1 semana en el peor caso). Upgrade a Pro ($25/mes) sigue siendo la recomendación definitiva a mediano plazo. |
| DEP-01 | 🟡 Moderada (aviso), baja en la práctica | `uuid <11.1.1` (buffer bounds check, GHSA-w5hq-g745-h8pq) transitivo vía `firebase-admin@12.7.0`. No explotable en este proyecto: `firebase-admin` solo se usa para Cloud Messaging (`_push.js`), nunca se llama `uuid.v3/v5/v6` con buffer propio. | 10 | 🟡 **Documentado, sin aplicar** — fix requiere bump de major (`firebase-admin` 12→14), probar `admin.messaging()` antes de mergear. No urgente por no ser explotable hoy. |
| DEP-02 | 🟢 Baja (higiene de build) | `esbuild` (devDependency, usado en `npm run build`) no tiene entrada en `package-lock.json` — `npm ci --dry-run` falla. Vercel usa `npm install` por defecto (no `npm ci`), así que no rompe el deploy hoy, pero la versión instalada no está fijada y rompería si algún día se agrega un paso de CI con `npm ci`. | 10 | 🔴 **Pendiente** — 1 comando (`npm install` + commit del lockfile actualizado) en la próxima sesión de código; no accionable desde acá (solo acceso al ZIP, no al repo real). |
| **OBS-03** | 🔴 **ALTA (funcional, no seguridad)** | **Las notificaciones push de "nuevo pedido" y "stock crítico" fallan al 100%, en silencio.** `internal_secrets.internal_push_secret` seguía sin fila el 2026-08-25 (reconfirmado en vivo — la carga anterior se perdió o nunca se completó). Los triggers tragan el error (`EXCEPTION WHEN OTHERS THEN NULL`, correcto para no bloquear la venta) pero eso también significa que nadie se enteró nunca. | 8 | 🟡 **DB corregida otra vez (2026-08-25)** — secreto de 64 hex regenerado e insertado en `internal_secrets` vía Supabase. **Falta que el usuario setee `INTERNAL_PUSH_SECRET` en Vercel (Project → Settings → Environment Variables, Production) con el mismo valor y redespliegue** — el conector de Vercel no puede leer ni escribir env vars, ese paso es manual. |
| **SEC-006** | 🔴 **ALTA** | **`rpc_registrar_devolucion_pos` no valida que la venta pertenezca a la empresa del usuario que llama.** Cualquier usuario autenticado podía procesar devoluciones (afecta stock y montos) de ventas de **otras empresas** del SaaS conociendo el UUID de la venta. | 2 | ✅ **Corregido 2026-07-11** — migración `fix_sec006_aislamiento_empresa_devolucion_pos` aplicada en Supabase. Verificado: el handler backend (`lib/handlers/pos.js`) ya validaba a nivel app, pero la función quedaba igual expuesta por RPC directo (patrón confirmado en `frontend/admin/js/pedidos.js`, que sí llama RPCs de Supabase sin pasar por el backend) — por eso el fix a nivel función era necesario igual. |
| SEC-007 | 🟠 Media | `onboarding_empresa(empresa_uuid)` no validaba que el caller fuera el superadmin del SaaS — permitía escrituras (registros default) en cuentas ajenas desde cualquier usuario autenticado. | 2 | ✅ **Corregido 2026-07-11** — migración `fix_sec007_autorizacion_onboarding_empresa`. Se usó `is_saas_owner()` (no `assert_empresa_access`) porque el único caller real es el panel superadmin (`frontend/admin/superadmin.html`) creando empresas nuevas — el caller nunca pertenece todavía a la empresa que está dando de alta. |
| SEC-008 | 🟠 Media | `cliente_productos_disponibles` expone catálogo completo (precios, stock) de cualquier empresa sin validar sesión — a confirmar si es diseño intencional (portal público) o falta de control. | 2 | ✅ **Corregido 2026-07-11 (sesión 9)** — decisión de negocio: restringirlo. Migración `fix_sec008_gate_catalogo_publico` (292) agrega guard `empresas.config->>'catalogo_publico_habilitado'` (default `false`) dentro de la función SQL (cierra el bypass directo por PostgREST, confirmado `GRANT EXECUTE` a `anon`/`authenticated`); mismo chequeo agregado en `resolverEmpresaCliente` (`lib/handlers/stock.js`) para el fallback `?empresa_id=`. Verificado: `productos`/`categorias` no eran vector (RLS ya filtra por `get_empresa_id()`). Ver `CHANGELOG_v296_fix_sec008_catalogo_publico_gateado.md`. Breaking change intencional — opt-in por empresa, ninguna de las 2 empresas reales de la base tenía el flag activo.|
| SEC-001 | Media | ~90 funciones `SECURITY DEFINER` quedan expuestas por `/rest/v1/rpc/*` a roles `anon`/`authenticated`. Se verificaron 33 de ellas: 30 con autorización interna correcta, 2 con fallas (SEC-006, SEC-007), 1 con diseño a confirmar (SEC-008). Faltan ~55. | 2 | 🟡 Verificación parcial (33/~88) |
| **SEC-010** | 🔴 **ALTA (sistémica)** | **Módulo completo del wizard de migración (`migracion_*`): ~18 de ~24 funciones `SECURITY DEFINER` recibían `p_empresa_id` sin validarlo contra el caller.** Cualquier usuario autenticado de cualquier empresa podía escribir (cheques, pedidos, lotes, cta_cte, comprobantes históricos, direcciones, órdenes de compra, pagos a proveedores, precios de cliente, proveedores, puntos, ventas POS) o **borrar** (`migracion_deshacer_sesion`) datos de **otra empresa** pasando su `empresa_id`. Es el mismo bug de SEC-006/SEC-007 repetido ~18 veces en un módulo entero que no se había auditado antes. | 2 | ✅ **Corregido 2026-07-11** — 4 migraciones (`fix_sec010_lote1..4`) agregan el chequeo estándar `auth.role() <> 'service_role' AND p_empresa_id IS DISTINCT FROM get_empresa_id()` a las 18 funciones afectadas, sin tocar el resto de la lógica. Verificado post-aplicación con `pg_get_functiondef` sobre las 32 funciones `migracion_*`. Los 4 helpers de puro formateo de texto (`migracion_formatear_cuit`, `migracion_normalizar_condicion_iva`, `migracion_normalizar_estado_pedido`, `migracion_parsear_dias_reparto`) no requieren el chequeo porque no reciben `empresa_id` ni tocan tablas. |
| SEC-009 | Baja | Patrón repetido: varias funciones (`anular_venta_pos`, `registrar_cobro_completo`, `recepcionar_orden_compra`, `rpc_registrar_devolucion_pos`) confían en `p_usuario_id` provisto por el cliente en vez de forzar `auth.uid()` — permite falsificar quién hizo la acción en el registro de auditoría (no es escalación de privilegios). | 2 | ✅ **Corregido 2026-07-11 (sesión 9)** — decisión de negocio: sí corregir. Migración `fix_sec009_forzar_auth_uid_p_usuario_id` (291) fuerza `p_usuario_id := auth.uid()` en las 4 funciones para cualquier caller que no sea `service_role`. Se preserva el caso legítimo del backend (`pedidos.js` pasando `chofer_id` en nombre del admin) porque corre con `SUPABASE_SERVICE_ROLE_KEY`. |
| SEC-002 | Baja | 3 tablas con RLS habilitado pero **sin ninguna política** (`asistente_articulos`, `asistente_uso`, `demo_snapshots`). | 2 | ✅ **Confirmado diseño intencional (2026-07-11, sesión 3)** — `asistente_articulos`/`asistente_uso` solo se tocan desde `lib/handlers/asistente.js` y `scripts/generar-embeddings-asistente.js`, ambos con `SUPABASE_SERVICE_ROLE_KEY` (bypasea RLS). `demo_snapshots` solo se toca desde las funciones `fn_snapshot_demo(_v2)`/`fn_reset_demo(_v2/_cron)`, todas restringidas a `service_role`. No hay ningún caller de cliente/frontend. **Hardening adicional 2026-07-11 (sesión 9)** — el diseño era correcto pero las 3 tablas conservaban el `GRANT` de tabla por defecto a `anon`/`authenticated` (redundante mientras RLS sin políticas siga activo, pero un riesgo latente si alguien agrega una política amplia sin pensarlo, o si RLS se deshabilita por error). Se aplicó `REVOKE ALL ... FROM anon, authenticated` como defensa en profundidad (`fix_sec015_revoke_table_grants_asistente_y_demo_snapshots`), mismo patrón que `198_revoke_table_grants_secret_tables` usó para `refresh_tokens`/`tokens_wsaa`. |
| SEC-003 | Baja | Protección de contraseñas filtradas (HaveIBeenPwned) **deshabilitada** en Supabase Auth. | 2 | 🔴 **Confirmado bloqueado por plan (2026-08-25)** — documentación oficial de Supabase: *"Leaked password protection is available on the Pro Plan and above"*. No es un toggle que falte activar, es una feature que no existe en Free. No accionable hasta upgrade a Pro; no es un fix pendiente de esta sesión. |
| SEC-004 | 🟢 Resuelta | Extensiones `pg_trgm` y `vector` estaban instaladas en schema `public` en lugar de uno dedicado — riesgo de colisión de nombres y de que una función nueva las use sin querer sin `search_path` explícito. | 2 | ✅ **Confirmado resuelto (verificado en vivo 2026-08-25)** — `pg_extension` muestra ambas en schema `extensions`, no en `public`. Se verificaron además las 6 funciones reales que usan operadores de trigram/vector (`buscar_articulos_asistente`, `buscar_clientes_asistente`, `buscar_productos_asistente`, `conciliar_oc_factura`, `conciliar_recepcion`, `migracion_precheck_advertencias` — no las ~29 que estimaba el mapeo original por heurística de texto, esa cifra incluía falsos positivos como `fn_productos_lista` que en realidad usa `ILIKE`, no trigram): las 6 tienen `SET search_path TO 'public, extensions'`, así que no se rompió nada al mover las extensiones. `get_advisors(security)` ya no reporta `extension_in_public`. Etapa 2 queda 100% cerrada. |
| SEC-005 | Baja | Función `fn_cheques_sync_vencimiento` sin `search_path` fijo (riesgo de search_path hijacking). | 2 | ✅ **Corregido 2026-07-11** — migración `fix_sec005_search_path_fn_cheques_sync_vencimiento`, agrega `SET search_path TO 'public'`. |
| **SEC-011** | 🟠 **Media** | **`saas_cron_activacion_check()` era invocable por `anon` y `authenticated` vía RPC sin ningún chequeo interno de autorización**, a diferencia de sus 3 funciones hermanas (`saas_cron_facturacion_mensual`, `saas_cron_suspender_morosos`, `saas_cron_trial_check`), todas restringidas a `service_role`. Es un job que opera sobre **todas las empresas del SaaS** (no un solo tenant): cualquiera, incluso sin login, podía forzar su ejecución repetidamente. No filtra datos ni escala privilegios (solo inserta en `saas_email_log` si no existe ya un registro de nudge por empresa), pero es una inconsistencia clara del patrón de seguridad del resto de los crons. | 2 | ✅ **Corregido 2026-07-11 (sesión 3)** — migración `fix_sec011_restringir_saas_cron_activacion_check_a_service_role`: `REVOKE EXECUTE ... FROM anon, authenticated; GRANT ... TO service_role`. Verificado con `has_function_privilege`. |
| **SEC-013** | 🟠 **Media** | **`verificarFirmaMP()` en `lib/handlers/pagos.js` falla ABIERTO si falta `WEBHOOK_SECRET_MP`** (devuelve `true` con solo un `console.warn`), a diferencia del webhook de WhatsApp (`notif.js`), que falla cerrado ante la misma situación con el mismo tipo de secreto. Si la env var faltara en producción, el endpoint del webhook de Mercado Pago aceptaría requests sin firma — el impacto está parcialmente acotado porque igual se re-consulta el pago real contra la API de MP con las credenciales de la empresa antes de acreditar nada, pero es una inconsistencia de "fail-open" que contradice el patrón que este mismo proyecto ya adoptó para el otro webhook. | 3 | ✅ **Corregido 2026-07-11 (sesión 5)** — `verificarFirmaMP()` ahora retorna `false` (rechaza con 401) si `WEBHOOK_SECRET_MP` no está configurado, mismo patrón fail-closed que `firmaValidaDeMeta()` en `notif.js`. Fix a nivel código (no requiere migración SQL); verificado con `node --check`. |
| SEC-014 | 🟡 Baja (higiene) | 1 fila en `integraciones_pago` con `access_token` sin cifrar (sin prefijo `v1:`) — token de test de sandbox Mercado Pago (`TEST-...`), integración inactiva (`activa=false`), sin ningún caller real. | 4 | ✅ **Corregido 2026-07-11 (sesión 6)** — fila eliminada directamente en producción (no había credencial real que preservar, no se justificaba un backfill de re-cifrado). Verificado post-fix: `integraciones_pago` en 0 filas totales, 0 sin cifrar. |
| SEC-012 | 🟡 Baja (higiene / defensa en profundidad) | ~35 funciones `SECURITY DEFINER` que manipulan datos reales de negocio (`ajustar_stock`, `anular_venta_pos`, `registrar_movimiento_cta_cte`, `crear_orden_compra`, `confirmar_pedido`, `fn_pedidos_lista`, `get_carrito_cliente`, `get_saas_panel_admin`, `migracion_superadmin_resumen`, etc.) tienen `GRANT EXECUTE` otorgado a `anon` (usuario sin sesión). Hoy no eran explotables porque `get_empresa_id()`/`auth.uid()` devuelven `NULL` para `anon` y el patrón estándar del proyecto falla cerrado — se verificó esto en 33+ funciones durante esta auditoría. Pero es un patrón frágil: alcanza con que una futura función nueva olvide el chequeo, o que se cambie la implementación de `get_empresa_id()`, para que la exposición a `anon` sea explotable de inmediato. | 2 | ✅ **Corregido 2026-07-11 (sesión 4)** — se revisó función por función el caller real en `frontend/`/`lib/handlers/` (login pre-auth vs. sesión ya iniciada) y se revocó `EXECUTE` de `anon` en las 25 funciones sin ningún caller legítimo sin sesión (`fix_sec012_revocar_exec_anon_funciones_de_negocio` + `fix_sec012_parte2_revocar_public_grant_residual`, esta última porque 7 de esas 25 heredaban el privilegio del pseudo-rol `PUBLIC`, no de un grant directo a `anon`, y el primer `REVOKE` no alcanzaba). Se mantuvo el acceso `anon` en: 10 *getters* de sesión (`get_empresa_id`, `get_rol_usuario`, `auth_usuario_id`, `auth_empresa_id`, `chofer_clientes_ids`, `es_admin`, `es_chofer`, `assert_empresa_access`, `is_saas_owner`, `auth_usuario_rol`) porque se usan dentro de **99 políticas RLS** con roles `public` y revocarlos rompería esa evaluación; y en `empresa_publica_actual` (logo/nombre en pantallas de login pre-auth, confirmado en código) y `validar_token_portal_proveedor` (acceso público por diseño vía token). `cliente_productos_disponibles` queda sin tocar, pendiente de la decisión de negocio de SEC-008. Verificado con `get_advisors(security)`: el warning `anon_security_definer_function_executable` ahora solo lista esas 13 funciones intencionales. |

---

| **SECNEW-01** | 🔴 **ALTA (sistémica, encontrada fuera de esta auditoría — v982)** | **`crear_pedido_cliente` (función agregada después de la última revisión de este plan) insertaba pedidos con el `empresa_id`/`cliente_id` que le pasara el caller, sin validar nada contra la sesión.** Expuesta a `anon`/`authenticated` por PostgREST (confirmado `has_function_privilege`), permitía crear pedidos falsos (con reserva real de stock) a nombre de **otra empresa** del SaaS — mismo patrón que SEC-006/SEC-010. | — (fuera de las 12 etapas originales) | ✅ **Corregido 2026-08-24** — migración `20260824070000_fix_secnew01_aislamiento_empresa_crear_pedido_cliente.sql` agrega el chequeo estándar (`auth.role() <> 'service_role'` + `p_empresa_id IS DISTINCT FROM get_empresa_id()` + validación de que `cliente_id` pertenezca a esa empresa). Verificado que el único caller real (`lib/handlers/pedidos.js`, `lib/repos/whatsapp-bot.js`, ambos vía `service_role`) queda sin cambios de comportamiento. Verificado con `pg_get_functiondef` post-fix. **Barrido sistemático hecho el mismo día (2026-08-24):** se revisaron las 229 funciones `SECURITY DEFINER` de `public` con acceso real a `anon`/`authenticated`, buscando las que reciben un parámetro de tenant (`p_empresa_id`/`p_cliente_id`/`p_usuario_id`) sin ninguna referencia a `get_empresa_id()`/`auth.uid()`/`auth.role()`/`is_saas_owner()`/`assert_empresa_access()`/`get_rol_usuario()` en su cuerpo. Aparecieron 2 más, ambas corregidas y verificadas en la misma sesión (ver `SECNEW-02` abajo): `conciliar_oc_factura` (fuga de datos de conciliación de compras de otra empresa) y `fn_generar_alertas_stock_autonomo` (escritura: generaba órdenes de compra falsas en cualquier empresa). Una tercera, `fn_asegurar_piso_reciente_demo`, no era explotable de fondo (exige `es_demo=true` internamente) pero se revocó igual por defensa en profundidad. Las 2 restantes de la lista (`setup_inicial_empresa`, `empresa_publica_actual`) se revisaron y son seguras por diseño: la primera es un bootstrap de singleton que ya quedó inerte (`empresas` no está vacía) y la segunda solo expone nombre/logo público, igual que sus hermanas `empresa_publica_por_id/slug`. El advisor de seguridad (`anon_security_definer_function_executable`) bajó de 33 a 30 funciones tras estos fixes — las 30 restantes ya estaban verificadas como intencionales en sesiones anteriores (getters de RLS, tokens públicos, catálogo/carrito con su propio chequeo de sesión, o las funciones de negocio que sí validan `p_empresa_id` correctamente como `alta_factura_proveedor`/`editar_factura_proveedor`/`registrar_pago_proveedor`/`fn_pedidos_lista`/`fn_crear_producto`/`get_carrito_cliente`/`crear_pedido_cliente` -ya corregida-). |
| **SECNEW-02** | 🟠 **Media (fuga de datos + escritura no autorizada, encontradas en el mismo barrido)** | **`conciliar_oc_factura`** exponía a `anon`/`authenticated` datos de conciliación de compras (cantidades, precios, discrepancias) de cualquier empresa sin validar `p_empresa_id` contra la sesión. **`fn_generar_alertas_stock_autonomo`** (función de escritura, sin ningún caller en el código ni en `pg_cron`) permitía generar órdenes de compra falsas en cualquier empresa del SaaS. **`fn_asegurar_piso_reciente_demo`** quedó expuesta sin necesidad (mitigada por diseño, igual corregida). | — (fuera de las 12 etapas originales) | ✅ **Corregido 2026-08-24** — migración `20260824070001_fix_secnew02_revocar_funciones_expuestas_sin_caller.sql`, con `REVOKE EXECUTE ... FROM PUBLIC` directo (cubre el privilegio heredado del pseudo-rol `PUBLIC` en un solo paso, sin necesitar una parte2 separada como en SEC-012). Verificado con `has_function_privilege`: `anon`/`authenticated` en `false`, `service_role` en `true` para las 3. Los únicos callers reales (`lib/repos/cc-proveedores.js` vía `service_role`, y las llamadas internas desde `alta_factura_proveedor`/`editar_factura_proveedor`, ambas propiedad de `postgres`) quedan sin cambio de comportamiento. |
| **PERF-07** | 🟠 **Media (posible bug de permisos, detectado al consolidar RLS)** | **`ordenes_compra` tenía 2 políticas `FOR ALL` simultáneas: `oc_modify` (restringida a `dueno/admin/depositero`) y `ordenes_compra_empresa` (`empresa_id = get_empresa_id()`, **sin ningún filtro de rol**). Como las políticas permisivas se combinan con OR, la segunda habilitaba a *cualquier* usuario autenticado de la empresa (vendedor, chofer, cliente de portal si tuviera fila en `usuarios`, etc.) a INSERT/UPDATE/DELETE en `ordenes_compra` — el chequeo de rol de `oc_modify` era letra muerta en la práctica. Mismo patrón exacto en `ordenes_compra_items` (`oc_items_empresa` sin rol + `oci_modify` con rol). No era un hallazgo de seguridad grave (todo quedaba dentro de la misma empresa, no había fuga cross-tenant), pero probablemente no era el permiso que se quería dar. | 7 | ✅ **Corregido 2026-07-19 (migración `oc_restrict_and_dedupe_407`)** — decisión de negocio confirmada por el usuario: restringir a `dueno/admin/depositero`, igual que el resto del módulo de compras. Se eliminaron las policies `ALL` sin filtro de rol (`ordenes_compra_empresa`, `oc_items_empresa`) y se reemplazaron las `ALL` restringidas (`oc_modify`, `oci_modify`, que duplicaban cobertura de `SELECT`) por policies separadas de `INSERT`/`UPDATE`/`DELETE`, dejando `oc_select`/`oci_select` como única vía de lectura — mismo patrón usado en v405/v406. Verificado: `pg_policies` muestra 4 policies por tabla (una por comando), `get_advisors(security)` no reporta ningún hallazgo RLS sobre `ordenes_compra`/`ordenes_compra_items`. |

## Historial de correcciones aplicadas en producción
| Fecha | ID | Migración | Detalle |
|-------|----|-----------|---------|
| 2026-07-11 | SEC-006 | `fix_sec006_aislamiento_empresa_devolucion_pos` | Agregado chequeo `auth.role() <> 'service_role' AND v_empresa_id IS DISTINCT FROM get_empresa_id()` en `rpc_registrar_devolucion_pos`, igual que `anular_venta_pos`. |
| 2026-07-11 | SEC-007 | `fix_sec007_autorizacion_onboarding_empresa` | Agregado chequeo `auth.role() <> 'service_role' AND NOT is_saas_owner()` en `onboarding_empresa`. Se investigó el único caller real (`superadmin.html`) antes de elegir el patrón correcto — no se usó `assert_empresa_access` porque hubiera roto el flujo legítimo (el superadmin no pertenece a la empresa que está creando). |
| 2026-07-11 | SEC-005 | `fix_sec005_search_path_fn_cheques_sync_vencimiento` | Agregado `SET search_path TO 'public'` a la función trigger `fn_cheques_sync_vencimiento`. |
| 2026-07-11 | SEC-010 | `fix_sec010_lote1_resolvers_y_mapear_bulk`, `fix_sec010_lote2_deshacer_sesion_y_maestro`, `fix_sec010_lote3_confirmar_lotes_parte1`, `fix_sec010_lote4_confirmar_lotes_parte2` | Se completó la verificación de TODO el módulo `migracion_*` (32 funciones) que no había sido cubierto en las etapas anteriores. Se encontraron 18 funciones sin aislamiento por empresa (la mayor cantidad de hallazgos en una sola pasada de esta auditoría) y se corrigieron todas con el patrón estándar del proyecto. `migracion_mapear_bulk` se convirtió de `LANGUAGE sql` a `plpgsql` para poder validar (deriva el `empresa_id` de la sesión, no de un parámetro). `migracion_deshacer_sesion` era la más grave por tener `DELETE`. `migracion_purgar_staging_antiguo` se restringió a `service_role` por ser un job de mantenimiento. Verificado post-aplicación con `get_advisors` que no se rompió nada. |
| 2026-07-11 (sesión 3) | SEC-011 | `fix_sec011_restringir_saas_cron_activacion_check_a_service_role` | `REVOKE EXECUTE ... FROM anon, authenticated` + `GRANT ... TO service_role` sobre `saas_cron_activacion_check()`, para alinearla con sus 3 funciones hermanas `saas_cron_*`. Verificado con `has_function_privilege`. |
| 2026-07-11 (sesión 4) | SEC-012 | `fix_sec012_revocar_exec_anon_funciones_de_negocio`, `fix_sec012_parte2_revocar_public_grant_residual` | 25 funciones de negocio revocadas de `anon` tras confirmar en el código (frontend/lib) que ninguna tiene un caller legítimo sin sesión. La segunda migración fue necesaria porque 7 de esas 25 (`ajustar_stock`, `anular_venta_pos`, `asentar_movimiento_cta_cte_factura`, `fn_notas_credito_lista`, `fn_notas_lista`, `fn_reportes_stock_distribucion`, `fn_reportes_stock_kpis`) tenían el privilegio heredado del pseudo-rol `PUBLIC` (visible en `proacl` como `=X/postgres`), no un grant directo a `anon` — el primer `REVOKE ... FROM anon` no alcanza en ese caso, hace falta `REVOKE ... FROM PUBLIC` explícito. Se detectó porque el primer chequeo post-fix mostró que 7 de las 25 seguían con `anon_exec = true`. Verificado con `get_advisors(security)` — el warning `anon_security_definer_function_executable` quedó reducido a las 13 funciones intencionales (10 getters usados en 99 políticas RLS + 3 públicas por diseño). |
| 2026-07-11 (sesión 6) | SEC-014 | `fix_sec014_borrar_token_test_sin_cifrar` | Se completó la Etapa 4 (revisión de `whatsapp-pedido-tools.js`, `asistente-providers.js`, `asistente-tools.js` — sin hallazgos). Se resolvió el único hallazgo pendiente (SEC-014): la fila de test sin cifrar en `integraciones_pago` se eliminó directamente en producción, dado que no era una credencial real y el usuario optó por borrarla en vez de dejarla pendiente de re-guardado manual. Verificado post-fix con SQL directo: 0 filas en la tabla. |
| 2026-07-19 | PERF-07 | `oc_restrict_and_dedupe_407` | Cierre del caso especial de la Etapa 7: el usuario confirmó que `ordenes_compra`/`ordenes_compra_items` deben restringirse a `dueno/admin/depositero`, igual que el resto del módulo de compras. Se eliminaron las policies `ALL` sin filtro de rol (`ordenes_compra_empresa`, `oc_items_empresa` — el agujero real de PERF-07) y se dividieron las policies `ALL` restringidas (`oc_modify`, `oci_modify`) en `INSERT`/`UPDATE`/`DELETE` separadas, dejando `oc_select`/`oci_select` como única policy de lectura — mismo patrón de v405/v406. Verificado con `pg_policies` (4 policies por tabla, una por comando) y `get_advisors(security)` (sin hallazgos RLS en ninguna de las 2 tablas). Con esto la Etapa 7 queda 100% cerrada. |

**⚠️ Nota importante sobre esta tabla:** todas las filas de SEC-005 a SEC-012/SEC-014 son migraciones SQL aplicadas **directamente sobre la base de Supabase en producción** vía la integración — tienen efecto inmediato, sin necesidad de deploy. La fila de la sesión 7 es la excepción: es 100% código de frontend (sin migración SQL), así que sigue el mismo régimen que SEC-013 — no tiene efecto hasta el próximo deploy.

| 2026-07-11 (sesión 7) | — | (sin migración SQL) | Etapa 5 (Frontend) sincronizada: el ZIP de arranque de esta sesión traía un checkpoint anterior a los fixes de XSS ya reportados. Se reaplicaron a `admin/clientes.html`, `admin/proveedores.html`, `admin/js/clientes.js`, `admin/js/riesgo-cheques.js` y `admin/js/comparador-precios.js` (que seguían con el `onclick` vulnerable) los mismos fixes ya verificados en `remito.js`/`adminlte-widgets.js`/`migracion.js`/`saas-billing.html`. Verificado con `node --check` (6 JS + 3 bloques `<script type="module">`) y test funcional de 3 payloads de inyección. Sin cambios en Supabase — es 100% frontend/código.

| 2026-07-11 (sesión 9) | — / SEC-002 | `fix_sec015_revoke_table_grants_asistente_y_demo_snapshots` | Cierre de un drift entre repo y producción: las 12 migraciones `fix_sec005`..`fix_sec014` (sesiones 2-6) estaban aplicadas en Supabase pero nunca se habían escrito como archivo en `supabase/migrations/` — se reconstruyeron los 11 archivos reales (279-289, con SEC-013 sin backfill porque fue un fix de código, no de DB) a partir de `pg_get_functiondef`/`supabase_migrations.schema_migrations`. Al re-correr `get_advisors(security)` como verificación de cierre apareció un hallazgo nuevo no documentado antes (SEC-002, hardening — ver fila arriba); se investigó, confirmó seguro y corrigió en el momento (290). También se completó el mapeo de funciones para SEC-004 (ver fila arriba) y se determinó que el `search_path` fijo en 29 funciones lo vuelve una migración de mayor riesgo — queda pendiente de decisión, no se tocó. |
| 2026-07-11 (sesión 9) | SEC-009 | `fix_sec009_forzar_auth_uid_p_usuario_id` (291) | Forzado `p_usuario_id := auth.uid()` para callers no-`service_role` en `anular_venta_pos`, `registrar_cobro_completo`, `recepcionar_orden_compra`, `rpc_registrar_devolucion_pos`. Confirmado en código que `registrar_cobro_completo` se llama directo desde el frontend (`cta-cte.js`, `rutas-resumen.js`) sin pasar por el backend — ese era el vector real. |
| 2026-07-11 (sesión 9) | SEC-008 | `fix_sec008_gate_catalogo_publico` (292) | Guard `empresas.config->>'catalogo_publico_habilitado'` (default `false`) agregado dentro de `cliente_productos_disponibles` (cierra el bypass directo por PostgREST — `GRANT EXECUTE` confirmado a `anon`/`authenticated`) y en `resolverEmpresaCliente` (`lib/handlers/stock.js`, código — no tiene efecto hasta deploy). Ver `CHANGELOG_v296_fix_sec008_catalogo_publico_gateado.md`. |

**SEC-013 es distinto: es un fix de código** (`lib/handlers/pagos.js`), no una migración SQL. Está corregido en este repositorio/ZIP y verificado con `node --check`, pero **NO tiene efecto hasta que hagas deploy** (git push / Vercel) — a diferencia de los fixes de base de datos, no hay forma de "aplicarlo" directo desde acá. Ver detalle en `etapas/03_backend_api.md`.

## Verificación consolidada de funciones `SECURITY DEFINER` (sesión 3, 2026-07-11)
Se listaron con SQL directo contra `pg_proc`/`has_function_privilege` **todas** las funciones `SECURITY DEFINER` de `public` (no una muestra) y se cruzaron contra lo ya verificado en sesiones anteriores. De las que quedaban sin verificar y tenían exposición real a `anon`/`authenticated` con parámetro `p_empresa_id`/`p_cliente_id`, se inspeccionó el código fuente (`pg_get_functiondef`) de: `analizar_stock_predictivo`, `calcular_ciclos_cliente`, `calcular_score_cliente`, `obtener_sugeridos_para_whatsapp`, `siguiente_numero_comprobante`, `migracion_alta_stock`, `migracion_confirmar_clientes_lote`, `migracion_confirmar_productos_lote`, `migracion_resolver_categoria`, `migracion_set_direccion_principal`, `migracion_confirmar_sesion`, `migracion_superadmin_resumen` — **las 12 tienen el chequeo estándar correcto**. Confirma que el cierre del módulo `migracion_*` de la sesión anterior era efectivamente completo.

También se revisó el **contenido real** (no solo la existencia) de las políticas RLS en `clientes`, `empresas`, `facturas`, `usuarios`, `cta_cte`, `tokens_wsaa`, `internal_secrets`, `refresh_tokens`. **Ninguna tiene agujeros tipo `USING (true)`** — todas filtran por `empresa_id`/`auth.uid()`/rol correctamente. Hallazgo menor de higiene: `tokens_wsaa` tiene 2 políticas `ALL` redundantes con el mismo efecto — se puede eliminar una, no es urgente.

## Próximos pasos inmediatos (orden sugerido)
1. Confirmar con el dueño del negocio si SEC-008 (`cliente_productos_disponibles`) es diseño intencional (catálogo público sin login) o hay que restringirlo — sesión 5 encontró evidencia adicional en `stock.js` (`handleClienteProductos`/`handleClienteCategorias`) de que el patrón de catálogo público es usado a propósito en más de un lugar, lo que apoya que sea diseño intencional.
2. SEC-003: activar manualmente en el dashboard de Supabase Auth (no accionable por SQL).
3. SEC-004: mapear funciones que usan operadores de `pg_trgm`/`vector` antes de mover las extensiones (riesgo re-evaluado, no es un fix de 1 click).
4. Decidir si se corrige SEC-009 (patrón `p_usuario_id` espejo).
5. ✅ **Hecho (sesión 5)** — `scripts/audit-security-grants.js` corrido contra la base real vía SQL directo. Los 8 hallazgos que reportó son falsos positivos confirmados (limitación de su heurística: solo reconoce filtro por `empresa_id`, no `auth.uid()` ni `is_saas_owner()`). Sin hallazgos de vistas ni de `search_path`. Etapa 2 queda 100% cerrada en lo accionable por SQL.
6. ✅ **Hecho (sesión 5)** — Etapa 3 completa: 34/34 handlers, 0 hallazgos abiertos (SEC-013 corregido en código, pendiente de deploy — ver nota abajo).
7. ✅ **Hecho (sesión 6)** — Etapa 4 completa: AFIP/ARCA, WhatsApp (incluyendo `whatsapp-pedido-tools.js`), email, Mercado Pago y asistente de IA revisados. SEC-014 resuelto (fila de test eliminada).
8. ✅ **Deployado (2026-07-12)** — SEC-013, los fixes de XSS, RL-01 original (v297) y su revert (v303) ya están en producción. **`v303` es el baseline deployado confirmado hoy.**
8b. **Recordatorio pendiente de deploy — vigente ahora:** `v304` (etapas 13-18 de esta misma auditoría, 21 hallazgos: `lib/handlers/pedidos.js`, `frontend/admin/js/migracion.js`, `ui-utils.js`, `rutas.js`, `rutas-resumen.js`, `cheques.js`, `cta-cte.js`, `notas.js`, `saas-billing.html`, `clientes.js`, `notif-log.js`) **y la Etapa 12** (`frontend/admin/login.html`, `frontend/admin/dashboard.html`, `lib/handlers/score.js`) están en este repo pero **no tienen efecto hasta el próximo `git push`/deploy a Vercel** — las migraciones SQL correspondientes a v304 ya están aplicadas directo en Supabase, solo falta el código.
9. ✅ **Hecho (sesión 7)** — Etapa 5 (Frontend) cerrada y sincronizada: 8 hallazgos de XSS corregidos y verificados en los 9 archivos afectados (el ZIP de arranque solo traía 4 de los 9 ya actualizados). 0 hallazgos abiertos. Queda pendiente, sin urgencia (documentado en `etapas/05_frontend.md`): auditar ~15 usos de `onclick` restantes con UUID (bajo riesgo) y unificar el patrón de escape (`sanitize`/`s`/`esc`/`escHtml`/`escapeHtml` re-declarados por archivo).
10. ✅ **Hecho (sesión 8)** — Etapa 1 (Inventario) cerrada: dependencias auditadas contra CVEs conocidas vía `web_search` (sin acceso a `registry.npmjs.org` desde este sandbox — `npm audit` falla por allowlist de red), `jsonwebtoken`/`node-forge` confirmados en versiones parcheadas; 0 secretos hardcodeados en backend; se leyó `AUDITORIA_SEGURIDAD_DISTRIB_v194.md` y se verificó contra el código actual — de sus 6 hallazgos, 3 ya estaban corregidos (CSP, exposición de `err.message`, y lo que se solapa con Etapa 5), 2 siguen sin cerrar como deuda técnica de bajo riesgo (BUG-01: fallback silencioso de `sanitize`; BUG-02: mayoría de módulos admin bypasea `api-client.js` y pega directo a PostgREST — no explotable hoy porque Etapa 2 ya verificó que RLS cubre esas tablas, pero rompe la capa de validación de negocio de los handlers).
11. Próximo bloque sugerido: **Etapa 6** (Consistencia E2E, no iniciada — buen lugar para BUG-01/BUG-02 de arriba) o cerrar los pendientes puntuales de Etapa 2 (SEC-003 manual, SEC-004 mapeo de `pg_trgm`/`vector`, SEC-008/SEC-009 decisión de negocio) — a definir con el usuario.

| 2026-08-25 | — (higiene) | `cleanup_fantasma_api_rate_limits_v297` | Se encontraron `public.api_rate_limits` (tabla) y `fn_rate_limit_check` (función) como restos muertos del intento de RL-01 v297, revertido en v303 y reemplazado en v982 por `rate_limits`/`rl_check_and_increment`. 0 filas, sin ningún caller en el código actual, sin `EXECUTE` para `anon`/`authenticated`. A pedido del usuario, se borraron ambos objetos. Verificado post-borrado: ninguno existe ya en `pg_tables`/`pg_proc`. |

## Cómo continuar en una sesión nueva
Decime "segui con la auditoría" y retomo desde la tabla de estado de este archivo — no hace falta repetir contexto.
